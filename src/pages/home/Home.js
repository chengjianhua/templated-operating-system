import React, { Component } from 'react';

import s from './Home.css';

export default class Home extends Component {
  render() {
    return (
      <div
        className={s.root}
      >
        Homepage.
      </div>
    );
  }
}
